<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS only -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
      
    <title>URL Shorter</title>
    <style type="text/css">
    body {
        font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
        
    }
    .url {
        width: 100%;
        height: 50px;
        line-height: 50px;
        text-align: center;
        background: #111;
        color: #eee;
        font-weight: 600;
    }
    #form{

        padding: 30px 20px;
        background: #0e5b8b;
        border-radius: 18px;
     }
    .label{
        color:rgb(234, 0, 255) ;
  
    }
    .tab{
        font-family: cursive;
        color: rgb(243, 169, 9);
        cursor: crosshair;
    }
    
    #tble{
        font-size: medium;
       background-color: aliceblue;
    }

    .message .success{
        font-size: 20px;
        color: green;
        position: absolute;
        animation: buttons .3s linear;
        display: none;
    }
    </style>
</head>
<body>
    <div class="container">
   
    <h1 class="url">URL Shorter</h1>
    <form id="form" action="/shortUrls" method="POST" class="my-4 form-inline">
        

        <label class="label" for="fullUrl">Enter the Full URL</label><br><br>
       

        <input required placeholder="URL"
         type="url"
         name="fullUrl"
         id="fullUrl"
         class="form-control col mr-2">
         <br><br>

        <button class="btn btn-success" type="submit">Create New !!</button>
      
    </form>


    <table id="tble"  class="table table-striped table-responsive">
        <thead>
            <tr>
                <th>Full URL</th>
                <th>Short URL</th>
                <th>Total Visits</th>
            </tr>
        </thead>
        <tbody class="tab">
            <% shortUrls.forEach(shortUrl => { %>
                <tr>
                    <td><a href="<%= shortUrl.full %>"><%= shortUrl.full %></a></td>
                    <td><a href="<%=shortUrl.short %>"><%=shortUrl.short %></a></td>
                    <td><%= shortUrl.clicks %></td>
                </tr>

            <% }) %> 
            
           
        </tbody>
    </table>
</div>
</body>
</html>
